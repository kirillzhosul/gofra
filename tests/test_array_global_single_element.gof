/// Test that storing and loading from global static array returns same result

#define TESTKIT_EXPECTED_EXIT_CODE 69

var exit_code int[1]

func int main[]
    &exit_code[0] TESTKIT_EXPECTED_EXIT_CODE !<

    exit_code[0] return
end