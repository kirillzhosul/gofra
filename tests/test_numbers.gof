#include "std.gof"

func void main
    0        drop
    128      drop
    1024     drop
    16777216 drop

    0xFF 255      "Hex must equals to same value in 10-base " assert
    0xFFFF 65535  "Hex must equals to same value in 10-base " assert

    // Max 16 bytes (64 bits) register
    // This is probably is 64bits arch related
    18446744073709551615 drop
end