/// Test simple N iterations with while loop
#include "os/general.gof"
#include "assert.gof"

const ITERATIONS = 10

func void main[]
    var x = 0;

    while x ITERATIONS < do
        &x x 1 + !< 
    end 
    
    x ITERATIONS "While loop must perform exactly N iterations" call assert

end