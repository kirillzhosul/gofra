#include "os/general.gof"
#include "assert.gof"

#define ITERATIONS 10

func void main[]
    0 while copy ITERATIONS < do
        1 + 
    end ITERATIONS "While loop must perform exactly N iterations" assert
    0 exit
end